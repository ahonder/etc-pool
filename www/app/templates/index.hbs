<div class="jumbotron-brand">
  <div class="container">
    <div class="row">
      <div class="col-md-5">
        <div class="row">
          <h1 style="font-size: 24px">
            <div class="etc-green">
              <strong><span class="logo-2">ETCTürk Madenci Havuzu</span></strong>
            </div>
          </h1>
        </div>
        <div class="row" style="padding:15px 5px;">
          <strong>Min. Ödeme: {{config.PayoutThreshold}}</strong>, Gün içinde sürekli ödemeler.
          <br/>
          <span class="label label-success">PPLNS</span> Düzenli ve kazançlı ödeme sistemi.
        </div>
      </div>
      <div class="col-md-3 stats">
        <div><i class="fa fa-users"></i> Bağlı Madenci: <span id="poolHashrate">{{format-number stats.model.minersTotal}}</span></div>
        <div><i class="fa fa-tachometer"></i> Havuz Hash Gücü: <span id="poolHashrate">{{format-hashrate stats.model.hashrate}}</span></div>
        <div><i class="fa fa-money"></i> Komisyon: <span id="poolFee" class="label label-success">{{config.PoolFee}}</span></div>
        {{#if stats.model.stats.lastBlockFound}}
          <div><i class="fa fa-clock-o"></i> Son Blok: <span>{{format-relative (seconds-to-ms stats.model.stats.lastBlockFound)}}</span></div>
        {{/if}}
      </div>
      <div class="col-md-4 stats">
        <div><i class="fa fa-unlock-alt"></i> Ağ Zorluğu: <span>{{with-metric-prefix stats.difficulty}}</span></div>
        <div><i class="fa fa-tachometer"></i> Ağ Hash Gücü: <span>{{format-hashrate stats.hashrate}}</span></div>
        <div><i class="fa fa-bars"></i> Blokzincir No: <span>{{format-number stats.height}}</span></div>
        <div><i class="fa fa-clock-o"></i> Geçerli Şans: <span>{{format-number stats.roundVariance style='percent'}}</span></div>
        <div><i class="fa fa-credit-card-alt fa-fw"></i> Anlık Fiyat: <span>$ {{model.exchangedata.current_price}}</span></div>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="stats">
    <h4>İstatistik &amp; Ödeme Geçmişiniz</h4>
    <div class="input-group">
      {{input value=cachedLogin class="form-control" placeholder="Ethereum Classic Adresinizi Girin"}}
      <span class="input-group-btn">
        <button class="btn btn-primary btn-etc" type="button" {{action 'lookup' cachedLogin}}>
          <span style="display: inline;"><i class="fa fa-search"></i> Gözat</span>
      </button>
      </span>
    </div>
  </div>
  <div>
<div class="container">
  {{high-charts mode=mode chartOptions=chartOptions content=chartData}}
</div>
  <div class="container">
  {{high-charts mode=mode chartOptions=chartDiff content=chartData}}
</div>
    {{#if (equals config.Network 'mordor')}}
      <div class="bs-callout bs-callout-danger">
        <h4>
          Warning
        </h4>
        <p>This is an experimental pool running on the <strong>Mordor</strong> testnet! All mined coins are for testing purposes only.</p>
      </div>
    {{/if}}
    <div class="jumbotron">
      <div class="container">
        <h3 class="text-center" style="padding: 0 0 5px 0; margin: 0 0 40px 0;"> Instructions</h3>
        <div class="row">
          <div class="col-md-12">
            <p class="lead text-center">
              <a href="#/help" class="ul-link">
                <span class="ul ul-danger">stratum</span><br />
                <code>{{config.StratumHost}}:{{config.StratumPort}}</code>
              </a>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
